# Where the project stands

> Generated by `docs/testing/build_timetable.py` on 2026-09-17. Do not edit by hand — change `docs/testing/features.csv` and rebuild.

**42 of 63 features complete (67%)** · 137 unit tests, 0 failing · 46 integration checks · 6 open defects

## By module

| Module | Done | Owner |
|---|---|---|
| WORD PUZZLE GAME ENGINE | 10/11 — started | Mau |
| CULTURAL CONTENT | 3/5 — started | Kyla |
| GAMIFICATION | 9/10 — started | Alyssa |
| BLOCKCHAIN INCENTIVE | 14/15 — started | Alyssa |
| ADMINISTRATIVE DASHBOARD | 0/5 — not started | Yow |
| AUTHENTICATION AND IDENTITY | 5/6 — started | UNASSIGNED |
| CLOUD INFRASTRUCTURE AND SECURITY | 0/8 — not started | Kyla |
| TESTING, QA AND DOCUMENTATION | 1/3 — started | Yow |

## In progress (7)

- **Dish content API endpoint** — Kyla · get_levels.php serves word and trivia. Full 6-field content endpoint outstanding. Levels table realigned to the dataset by migration 009 (27 rows, ids kept 1-27 so attempts and unlocks keep their foreign keys). api/lib/islands.php regenerated to match. The phone still reads levels locally - the endpoint serving the full 6-field content is what is outstanding.
- **User CRUD and search** — Yow · ASP.NET Web Forms CRUD exists from Capstone 1. Search and Capstone 2 refresh outstanding.
- **Dish/content CRUD (all 6 cultural fields)** — Yow · Blocked on the validated dataset.
- **Role-based access enforcement (Player vs Admin)** — Yow · Backend ready: users.role (migration 004) and kk_require_admin(). Dashboard enforcement outstanding.
- **Unit tests per module (5-module breakdown)** — Yow · Modules 1 and 2 fully covered: 82 tests, 0 failures. Module 3 PHP runner 51 passed 0 failed on 2026-08-24. Modules 4 and 5 pending those modules.
- **Clean Architecture layering for the web admin** — Yow · KusinaKode.Domain assembly (netstandard2.0, zero dependencies) with 20 unit tests; repository interfaces; AdminDashboardService; presentation ViewModel. MVVM is not possible in Web Forms — this is Clean Architecture plus a Presentation Model.
- **Notifications - claimable reward, badge earned, streak reminder** — Mau · Built 2026-08-25 (Mau): KusinaNotifications with Rewards and Reminders channels, POST_NOTIFICATIONS runtime request, a notification when a badge finishes minting, and a daily-claim nudge on app open. NOT DONE: reminders while the app is closed (streak nudge, end-of-day daily warning) - those need WorkManager, which was deliberately not added. Verified permission prompt on emulator. In-app Notifications screen added behind the Home header bell - claimable rewards plus every on-chain reward with its TxHash, since system notifications vanish once dismissed. Bell count (D-41, 2026-09-16): daily claim is two waiting rows (KK + palayok), so the badge matches WAITING FOR YOU. Inbox (2026-09-16): minted title drops the bang and status is title-cased (Confirmed); Mark all read / Select / Archived sit on the right. On-chain list is newest-first (2026-09-17). Ledger ticket (D-43): report is a button, details are required, Done hides while filing. Junk like aaa is rejected (D-45: 12+ characters in the player's own words). Daily inbox title is Daily 2026-09-17; Recorded converts UTC to the phone's local time. Island complete (2026-09-17, D-46): CLAIMED chip plus waiting/claimed notifications, same as Daily Login. Palayok free spin (D-47, 2026-09-17): daily claim banks the spin; a claimed day missing one is repaired on the next pantry snapshot. Inbox (D-48, 2026-09-17): resume refreshes so a claimed daily moves to On-Chain Activity; Daily palayok spin stays in Waiting until the wheel is turned. After the wheel (D-49, 2026-09-17): pantry/spin.php writes a palayok_spin receipt (0 KK) so On-Chain Activity lists Daily login claim then Daily palayok spin. Waiting titles match those two names. Inbox chrome (2026-09-17): parchment cards matching Settings, one box per row, no circular wells.

## Needs a team decision (2)

- **Max-attempts scaling by word length** — Mau · NOT BUILT AS WRITTEN. Team decided 2026-08-10 on a flat 6 attempts for every dish. Amend this line or reinstate scaling before defense.
- **Downloadable badge certificate (PDF) on claim** — Alyssa · Built 2026-08-25 then REMOVED the same day at the team's request. The PDF generator, the on-screen certificate, the badge tap target and the FileProvider that shared the file are all gone; badges are display-only again. Nothing else depended on it. Re-add it here as Not Started if the decision is revisited - the TxHash it printed is still on every confirmed badge.

## Not started (12)

- **Expert-validated recipe content (Chef Gicana)** — Kyla · Content is now sourced and referenced per dish, but sourced is not endorsed: isValidated stays false and the KODEX still shows 'PREVIEW - expert validation in progress'. Chef Gicana's sign-off is what flips it.
- **Blockchain audit panel (log + transaction detail)** — Yow · Backend live-checked 2026-08-24 (admin 200, player 403). Dashboard UI still outstanding.
- **Azure AD B2C authentication and RBAC** — Kyla + Alyssa · Interim tokens are B2C-shaped so only auth.php changes.
- **Azure VM (PHP REST API + blockchain node)** — Kyla
- **Azure App Service (admin dashboard hosting)** — Kyla
- **Azure SQL Database (relational data)** — Kyla
- **Azure Cosmos DB (dish/recipe content)** — Kyla
- **Azure Blob Storage (images)** — Kyla
- **Azure Key Vault (secrets)** — Kyla · Seam built: config.php reads every secret from the environment with a local fallback, so no code changes to deploy.
- **HTTPS/TLS transport** — Mau · HIGH PRIORITY. Traffic is currently plaintext HTTP, so passwords and tokens are readable on the network.
- **Replace hard-coded LAN endpoint with the Azure host** — Mau · Defect D-19. App cannot reach the server after any network change.
- **Stress testing (API, DB, blockchain node, mobile rendering)** — Yow · 3 of 4 targets blocked. Mobile rendering is NOT blocked and can run today.

## Open defects

- **D-17** (High, Backend) — post_attempt / post_unlock / get_unlocks / get_profile_stats still accept a spoofed user_id
- **D-19** (High, Mobile) — App cannot reach the server after changing network
- **D-26** (Medium, Admin Dashboard) — Every ranked player shows Bronze — Gold and Silver are unreachable
- **D-29** (High, Backend / Chain) — PHP EC key generation fails on Windows XAMPP, so no custodial wallet can ever be created and every badge stays pending
- **D-31** (High, Blockchain) — Two rewards in flight at once both fail with 'Chain failed to sync after append', so the KK is never credited
- **D-32** (Medium, Blockchain) — A reward that fails once is stranded forever - it cannot resume and cannot be re-claimed, so the KK is silently lost

## Next actions

Derived from what is unblocked, not from a plan:

1. **Decide:** Max-attempts scaling by word length — NOT BUILT AS WRITTEN. Team decided 2026-08-10 on a flat 6 attempts for every dish. Amend this line or reinstate scaling before defense.
1. **Decide:** Downloadable badge certificate (PDF) on claim — Built 2026-08-25 then REMOVED the same day at the team's request. The PDF generator, the on-screen certificate, the badge tap target and the FileProvider that shared the file are all gone; badges are display-only again. Nothing else depended on it. Re-add it here as Not Started if the decision is revisited - the TxHash it printed is still on every confirmed badge.
1. **Assign an owner:** Player onboarding and How to Play tutorial (built, but in no signed module)
1. **Assign an owner:** Login and sign-up (mobile) (built, but in no signed module)
1. See `docs/testing/gaps.csv` for what has no test coverage.
