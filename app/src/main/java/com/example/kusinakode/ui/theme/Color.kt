package com.example.kusinakode.ui.theme

import androidx.compose.ui.graphics.Color

// --- Kusina Kode design system: Filipino kitchen warmth ---

// Primary palette
val DarkBrown = Color(0xFF6F3913)      // Buttons, top bars, key actions
val GrayBrown = Color(0xFF7E6A32)      // App bars, drawer, secondary surfaces
val LightOrange = Color(0xFFF1E2C6)    // Text on dark, cream accent
val Cream = Color(0xFFF2E4C9)          // Soft background tint
val Brown = Color(0xFF8D704E)          // Theme background base

// Screen headers — the dark brown panel every top bar shares
val HeaderTop = Color(0xFF5C2A12)
val HeaderBottom = Color(0xFF3D1A0A)

/** Region filter chips (All / Luzon / Visayas / Mindanao / Philippines). */
val RegionChipOn = Color(0xFFB05B31)
val RegionChipOff = Color(0xFFFBE4D1)
val RegionChipOffStroke = Color(0xFFE8C9B4)
val RegionChipInk = Color(0xFF53433B)

/** Thin pill progress — terracotta into tan, cream track. */
val ProgressFillStart = Color(0xFFB25E33)
val ProgressFillEnd = Color(0xFFE6BA82)
val ProgressTrackIdle = Color(0xFFF6DECA)
val ProgressGoldStart = Color(0xFF7B5C2E)
val ProgressGoldEnd = Color(0xFFE0B77F)

// Surfaces & cards
val CardSurface = Color(0xFFFFFDF9)    // Cards, form backgrounds
val CardSurfaceVariant = Color(0xFFF5EDE0)
val OverlayLight = Color(0xCCFFFFFF)   // Semi-transparent white (profile stats)

// Borders & outlines
val OutlineDefault = Color(0xFFD4C4A8)
val OutlineFocused = Color(0xFF8B6914)

// Feedback
val SuccessGreen = Color(0xFF2E7D32)
val ErrorRed = Color(0xFFC62828)
val HintGray = Color(0xFF6B5B4F)

// Game-specific (can still be used in GameScreen)
val DarkHintBrown = Color(0xFF5A3D00)
val DarkGreen = Color(0xFF1B5E20)
val LightGreen = Color(0xFFA5D6A7)

// Level select & list
val PastelGray = Color(0xFFE5C386)

// Completed screen
val GrayOrange = Color(0xFF997E5C)
